.venv/
__pycache__/
*.pyc

# Environment variables - never commit this
.env

# Chrome profile persists extension state and cookies - not for VCS
.chrome_profile/

# Persisted last-vote timestamps, used as fallback for sites without next-vote APIs
state.json
state.json.tmp
